# 🤖 n8n Workflow Automation — Полное руководство

> **Визуальное создание торговых стратегий без написания кода**

---

## 📖 Что такое n8n Workflow Automation?

**n8n** — это визуальная платформа автоматизации (как Zapier, но с открытым кодом и полным контролем над данными).

**Workflow (рабочий поток)** — это последовательность действий, которая выполняется автоматически при наступлении определённого события.

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Триггер   │ →  │   Действие  │ →  │   Условие   │ →  │   Результат │
│  (Событие)  │    │  (Запрос)   │    │  (Если/Или) │    │  (Ордер)    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

---

## 🎯 Примеры торговых workflow

### 1️⃣ Простой: Уведомление о цене Bitcoin

```
[Расписание: каждые 5 мин] 
         ↓
[HTTP Request: Binance API → BTC/USDT price]
         ↓
[IF: Price > $100,000]
         ↓
[Telegram: Отправить "Bitcoin пробил $100K!"]
```

**Визуальная схема:**

```
┌──────────────┐     ┌──────────────────┐     ┌──────────┐     ┌─────────────┐
│ ⏰ Schedule  │ ──→ │ 🌐 HTTP Request  │ ──→ │ 🔀 IF    │ ──→ │ 📱 Telegram │
│ Every 5 min  │     │ Binance API      │     │ > 100000 │     │  Send Alert │
└──────────────┘     └──────────────────┘     └──────────┘     └─────────────┘
```

---

### 2️⃣ Средний: RSI-стратегия с исполнением ордера

```
[Расписание: каждые 15 мин]
         ↓
[HTTP Request: Binance → BTC/USDT candles]
         ↓
[Code (JavaScript): Рассчитать RSI(14)]
         ↓
[IF: RSI < 30 → Перепроданность]
         ↓
[HTTP Request: Binance → BUY Market Order]
         ↓
[PostgreSQL: Записать ордер в БД]
         ↓
[Telegram: "Куплено 0.01 BTC по цене $X"]
```

**Визуальная схема:**

```
┌──────────────┐     ┌──────────────────┐     ┌─────────────┐
│ ⏰ Schedule  │ ──→ │ 🌐 HTTP Request  │ ──→ │ 💻 Code     │
│ Every 15 min │     │ Get OHLCV data   │     │ RSI(14)     │
└──────────────┘     └──────────────────┘     └─────────────┘
                                                  │
                    ┌─────────────────────────────┘
                    ↓
              ┌──────────┐
              │ 🔀 IF    │
              │ RSI < 30 │
              └──────────┘
                    │
         ┌──────────┴──────────┐
         ↓                     ↓
   ┌──────────────┐      ┌──────────────┐
   │ 🌐 HTTP      │      │ 🗄️ PostgreSQL│
   │ BUY Order    │      │ Log trade    │
   └──────────────┘      └──────────────┘
         │
         ↓
   ┌─────────────┐
   │ 📱 Telegram │
   │ Notification│
   └─────────────┘
```

---

### 3️⃣ Продвинутый: AI-анализ новостей + торговля

```
[Расписание: каждый час]
         ↓
[HTTP Request: News API → Crypto news]
         ↓
[OpenAI: Анализ тональности (Positive/Negative/Neutral)]
         ↓
[IF: Sentiment = Very Positive AND Volume > Threshold]
         ↓
[HTTP Request: Binance → BUY]
         ↓
[Notion: Записать в базу торговых решений]
         ↓
[Telegram: "AI рекомендует покупку: причина..."]
```

---

## 🔧 Основные типы нод в n8n

### Триггеры (Start)

| Нода | Описание | Пример |
|------|----------|--------|
| **Schedule** | Запуск по расписанию | "Каждые 5 минут", "В 9:00 утра" |
| **Webhook** | Запуск по HTTP-запросу | Получение данных от TradingView |
| **Telegram Trigger** | Запуск по сообщению | Команда /buy от пользователя |
| **Cron** | Запуск по cron-выражению | "0 */4 * * *" (каждые 4 часа) |

---

### Биржи и API

| Нода | Описание | Пример |
|------|----------|--------|
| **HTTP Request** | Универсальный HTTP-клиент | Запрос к любому API |
| **Binance** | Прямая интеграция с Binance | Получение цены, исполнение ордера |
| **Bybit** | Прямая интеграция с Bybit | Фьючерсы, спот |
| **CoinGecko** | Данные о криптовалютах | Рыночная капитализация, цены |

---

### Логика и условия

| Нода | Описание | Пример |
|------|----------|--------|
| **IF** | Ветвление по условию | Если RSI < 30 → покупать |
| **Switch** | Множественное ветвление | Если цена > X → A, если < Y → B |
| **Merge** | Объединение потоков | Сравнение данных из 2 источников |
| **Split In Batches** | Циклы и итерации | Обработка списка монет |

---

### Данные и преобразования

| Нода | Описание | Пример |
|------|----------|--------|
| **Code** | JavaScript-код | Расчёт индикаторов |
| **Function** | Функция преобразования | Форматирование данных |
| **Date & Time** | Работа с датой | Конвертация таймзоны |
| **Edit Fields** | Изменение полей | Добавление/удаление полей |

---

### Уведомления

| Нода | Описание | Пример |
|------|----------|--------|
| **Telegram** | Отправка сообщений | Алерты о сделках |
| **Email** | Отправка email | Дайджест за день |
| **Slack** | Отправка в Slack | Уведомления команды |
| **Discord** | Отправка в Discord | Алерты в канал |

---

### Базы данных

| Нода | Описание | Пример |
|------|----------|--------|
| **PostgreSQL** | Запросы к PostgreSQL | Логирование сделок |
| **MySQL** | Запросы к MySQL | Хранение истории |
| **MongoDB** | Работа с MongoDB | Гибкое хранение |
| **Redis** | Кэширование | Быстрый доступ к данным |

---

### AI и машинное обучение

| Нода | Описание | Пример |
|------|----------|--------|
| **OpenAI** | GPT-модели | Анализ новостей |
| **Hugging Face** | ML-модели | Классификация текста |
| **Google AI** | Gemini модели | Генерация отчётов |

---

## 📊 Как это выглядит в интерфейсе n8n

```
╔═══════════════════════════════════════════════════════════════════╗
║  n8n Workflow Editor                                              ║
╠═══════════════════════════════════════════════════════════════════╣
║                                                                   ║
║   ┌─────────────┐                                                 ║
║   │   ⏰        │  Schedule                                        ║
║   │  Trigger    │  Every: 5 minutes                               ║
║   └──────┬──────┘                                                 ║
║          │                                                        ║
║          ▼                                                        ║
║   ┌─────────────┐                                                 ║
║   │   🌐        │  HTTP Request                                    ║
║   │   GET       │  https://api.binance.com/api/v3/ticker/price   ║
║   │             │  symbol: BTCUSDT                                ║
║   └──────┬──────┘                                                 ║
║          │                                                        ║
║          ▼                                                        ║
║   ┌─────────────┐                                                 ║
║   │   🔀        │  IF                                             ║
║   │  Condition  │  price > 100000                                 ║
║   └──────┬──────┘                                                 ║
║          │                                                        ║
║          ▼                                                        ║
║   ┌─────────────┐                                                 ║
║   │   📱        │  Telegram                                       ║
║   │   Send      │  Chat ID: 123456789                            ║
║   │   Message   │  "BTC пробил $100,000!"                        ║
║   └─────────────┘                                                 ║
║                                                                   ║
║   [▶️ Execute Workflow]  [💾 Save]  [⚙️ Settings]                 ║
╚═══════════════════════════════════════════════════════════════════╝
```

---

## 📁 Примеры готовых workflow (JSON)

### Пример 1: BTC Price Alert

```json
{
  "name": "BTC Price Alert",
  "nodes": [
    {
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 5
            }
          ]
        }
      }
    },
    {
      "name": "Get BTC Price",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "GET",
        "url": "https://api.binance.com/api/v3/ticker/price",
        "qs": {
          "symbol": "BTCUSDT"
        }
      }
    },
    {
      "name": "Check Price",
      "type": "n8n-nodes-base.if",
      "parameters": {
        "conditions": {
          "number": [
            {
              "value1": "={{ $json.price }}",
              "operation": "largerEqual",
              "value2": "100000"
            }
          ]
        }
      }
    },
    {
      "name": "Send Telegram",
      "type": "n8n-nodes-base.telegram",
      "parameters": {
        "operation": "sendMessage",
        "chatId": "123456789",
        "text": "🚀 BTC пробил $100,000! Текущая цена: {{ $json.price }}"
      }
    }
  ],
  "connections": {
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Get BTC Price",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get BTC Price": {
      "main": [
        [
          {
            "node": "Check Price",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check Price": {
      "main": [
        [
          {
            "node": "Send Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
```

---

### Пример 2: RSI Trading Strategy (упрощённый)

```json
{
  "name": "RSI Trading Strategy",
  "nodes": [
    {
      "name": "Schedule",
      "type": "n8n-nodes-base.scheduleTrigger",
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 15
            }
          ]
        }
      }
    },
    {
      "name": "Get Candle Data",
      "type": "n8n-nodes-base.httpRequest",
      "parameters": {
        "method": "GET",
        "url": "https://api.binance.com/api/v3/klines",
        "qs": {
          "symbol": "BTCUSDT",
          "interval": "15m",
          "limit": 14
        }
      }
    },
    {
      "name": "Calculate RSI",
      "type": "n8n-nodes-base.code",
      "parameters": {
        "code": "// JavaScript код для расчёта RSI\nconst closes = $input.all().map(item => parseFloat(item.json[4]));\n\nlet gains = [], losses = [];\nfor (let i = 1; i < closes.length; i++) {\n  const diff = closes[i] - closes[i-1];\n  if (diff > 0) gains.push(diff);\n  else losses.push(-diff);\n}\n\nconst avgGain = gains.reduce((a, b) => a + b, 0) / 14;\nconst avgLoss = losses.reduce((a, b) => a + b, 0) / 14;\nconst rs = avgGain / avgLoss;\nconst rsi = 100 - (100 / (1 + rs));\n\nreturn [{ json: { rsi: rsi.toFixed(2), price: closes[closes.length-1] } }];"
      }
    },
    {
      "name": "Check RSI < 30",
      "type": "n8n-nodes-base.if",
      "parameters": {
        "conditions": {
          "number": [
            {
              "value1": "={{ $json.rsi }}",
              "operation": "smaller",
              "value2": "30"
            }
          ]
        }
      }
    },
    {
      "name": "Send Alert",
      "type": "n8n-nodes-base.telegram",
      "parameters": {
        "operation": "sendMessage",
        "chatId": "={{ $env.TELEGRAM_CHAT_ID }}",
        "text": "🔔 RSI сигнал!\n\nRSI: {{ $json.rsi }}\nЦена: {{ $json.price }}\n\nСигнал: ПЕРЕПРОДАННОСТЬ (возможна покупка)"
      }
    }
  ]
}
```

---

## 🎓 Пошаговое создание первого workflow

### Шаг 1: Открыть n8n

Перейдите по адресу: `https://trading.yourdomain.com`

### Шаг 2: Создать новый workflow

1. Нажмите **"Create new workflow"**
2. Дайте название: "BTC Price Alert"

### Шаг 3: Добавить триггер

1. Нажмите **"+"** (добавить ноду)
2. Выберите **"Schedule"**
3. Настройте интервал: **Every 5 minutes**

### Шаг 4: Добавить HTTP Request

1. Нажмите **"+"** → **"HTTP Request"**
2. Метод: **GET**
3. URL: `https://api.binance.com/api/v3/ticker/price`
4. Query Parameters:
   - `symbol` = `BTCUSDT`
5. Нажмите **"Execute Node"** для проверки

### Шаг 5: Добавить условие IF

1. Нажмите **"+"** → **"IF"**
2. Condition: **Number**
3. Value 1: `={{ $json.price }}`
4. Operation: **larger or equal**
5. Value 2: `100000`

### Шаг 6: Добавить Telegram

1. Нажмите **"+"** → **"Telegram"**
2. Operation: **Send Message**
3. Chat ID: `={{ $env.TELEGRAM_CHAT_ID }}`
4. Text: `🚀 BTC пробил $100,000! Цена: {{ $json.price }}`

### Шаг 7: Сохранить и активировать

1. Нажмите **💾 Save**
2. Переключите **"Active"** в ON
3. Готово! 🎉

---

## 💡 Лучшие практики

### ✅ Делайте

- Используйте **переменные окружения** для чувствительных данных
- Добавляйте **обработку ошибок** (Error Trigger)
- Логируйте все сделки в **PostgreSQL**
- Тестируйте workflow на **демо-счёте** перед реальным запуском
- Используйте **Retry** для временных ошибок API

### ❌ Не делайте

- Не храните API-ключи в тексте workflow
- Не запускайте без **лимитов** (Stop Loss)
- Не игнорируйте **обработку ошибок**
- Не используйте workflow для **высокочастотной торговли** (задержки)

---

## 🔗 Полезные ресурсы

| Ресурс | Описание |
|--------|----------|
| [Официальная документация](https://docs.n8n.io/) | Полная документация n8n |
| [Список всех нод](https://docs.n8n.io/integrations/builtin/) | 200+ встроенных интеграций |
| [Сообщество n8n](https://community.n8n.io/) | Форум и помощь |
| [Workflow Templates](https://n8n.io/workflows/) | Готовые шаблоны |
| [GitHub n8n](https://github.com/n8n-io/n8n) | Исходный код |

---

## 📚 Готовые workflow для трейдинга

### В папке `workflows/` этого репозитория:

| Файл | Описание |
|------|----------|
| `btc-price-alert.json` | Уведомление о цене Bitcoin |
| `rsi-strategy.json` | RSI-стратегия с сигналами |
| `ai-news-analysis.json` | AI-анализ новостей |
| `multi-coin-tracker.json` | Отслеживание нескольких монет |
| `tradingview-webhook.json` | Интеграция с TradingView |

---

## 🎯 Следующие шаги

1. **Изучите интерфейс n8n** — создайте простой workflow
2. **Импортируйте готовый пример** — из папки `workflows/`
3. **Настройте под себя** — измените параметры
4. **Протестируйте** — запустите вручную
5. **Активируйте** — включите автоматическое выполнение

---

<div align="center">

**Готово к созданию торговых стратегий! 🚀**

[⬆️ Вернуться к началу](#-n8n-workflow-automation--полное-руководство)

</div>
